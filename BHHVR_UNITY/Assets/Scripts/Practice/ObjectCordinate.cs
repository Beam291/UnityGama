using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectCordinate : MonoBehaviour
{
    ////public ObjectCordinate cubeCordinate;
    //public NetworkBehavior networkBehavior;
    
    //// Start is called before the first frame update
    //void Start()
    //{}

    //// Update is called once per frame
    //void Update()
    //{
    //    //setup network behavior reference
    //    networkBehavior = GameObject.Find("NetworkConnect").GetComponent<NetworkBehavior>();
        
    //    //Cordinate of the Cube, it will update each time cube change cordinate
    //    float x = transform.position.x;
    //    float y = transform.position.y;
    //    float z = transform.position.z;
        
    //    //set x y z to network
    //    networkBehavior.cubeX = x;
    //    networkBehavior.cubeY = y;
    //    networkBehavior.cubeZ = z;
    //}
}
